<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:proyectoFinal_YapaFoods.Models"
             x:Class="proyectoFinal_YapaFoods.Views.HistoryPage"
             Title="Mis Pedidos"
             BackgroundColor="#F5F5F5">

    <RefreshView Command="{Binding RefreshCommand}" IsRefreshing="{Binding IsBusy}">
        <CollectionView ItemsSource="{Binding MisFacturas}" Margin="15">
            <CollectionView.EmptyView>
                <Label Text="Aún no has realizado pedidos." 
                       HorizontalOptions="Center" VerticalOptions="Center" 
                       TextColor="Gray"/>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Factura">
                    <Frame Margin="0,5" Padding="15" CornerRadius="10" HasShadow="True">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.VerDetalleCommand}" CommandParameter="{Binding .}"/>
                        </Frame.GestureRecognizers>

                        <Grid ColumnDefinitions="*, Auto">
                            <VerticalStackLayout>
                                <Label Text="{Binding Fecha, StringFormat='{0:dd/MM/yyyy HH:mm}'}" 
                                       TextColor="#333" FontAttributes="Bold"/>
                                <Label Text="{Binding Id, StringFormat='Pedido #{0}'}" 
                                       TextColor="Gray" FontSize="12"/>
                            </VerticalStackLayout>

                            <Label Grid.Column="1" 
                                   Text="{Binding Total, StringFormat='${0:F2}'}" 
                                   TextColor="#D32F2F" 
                                   FontAttributes="Bold" 
                                   FontSize="18" 
                                   VerticalOptions="Center"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </RefreshView>
</ContentPage>