<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="proyectoFinal_YapaFoods.Views.ProfilePage"
             Title="Mi Perfil"
             BackgroundColor="White">

    <VerticalStackLayout Padding="30" Spacing="20">
        <Image Source="dotnet_bot.png" WidthRequest="120" HeightRequest="120" HorizontalOptions="Center"/>
        <Label Text="Bienvenido" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="Black"/>

        <BoxView HeightRequest="1" Color="LightGray" Margin="0,10"/>

        <Button Text="📜 Historial de Pedidos" 
                Command="{Binding GoToHistoryCommand}" 
                BackgroundColor="White" 
                TextColor="#333" 
                BorderColor="Gray" 
                BorderWidth="1"
                CornerRadius="10"/>

        <Button Text="📍 Mis Direcciones (Próximamente)" 
                BackgroundColor="White" 
                TextColor="Gray" 
                BorderColor="LightGray" 
                BorderWidth="1"
                CornerRadius="10"/>

        <Button Text="🚪 Cerrar Sesión" 
                Command="{Binding LogoutCommand}" 
                BackgroundColor="#FFCDD2" 
                TextColor="#D32F2F" 
                Margin="0,30,0,0"
                CornerRadius="10"/>
    </VerticalStackLayout>
</ContentPage>